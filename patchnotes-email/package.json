{
  "name": "",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "prisma generate && tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "pnpm clean && pnpm build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "db:pull": "prisma db pull",
    "db:generate": "prisma generate",
    "db:smoke-test": "npx tsx scripts/prisma-smoke-test.ts"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@prisma/adapter-mssql": "^7.4.0",
    "@prisma/client": "^7.4.0",
    "resend": "^6.9.2"
  },
  "devDependencies": {
    "@types/node": "25.x",
    "azure-functions-core-tools": "^4.x",
    "dotenv": "^17.3.1",
    "prisma": "^7.4.0",
    "rimraf": "^6.1.2",
    "typescript": "^5.0.0"
  }
}
